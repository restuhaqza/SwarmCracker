---
# Manager Role - Handlers

- name: reload systemd
  systemd:
    daemon_reload: yes

- name: restart swarmd-manager
  systemd:
    name: swarmd-manager
    state: restarted
    daemon_reload: yes
