# SwarmKit socket directory persistence
# Install to: /etc/tmpfiles.d/swarmkit.conf
# This ensures /var/run/swarmkit is created at boot with proper permissions

# Type Path               Mode UID GID Age Argument
d     /var/run/swarmkit  0755 root root  -   -
